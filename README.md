# keyroute
Keyroute is a lightweight, framework-agnostic keyboard shortcut router for modern web applications. Route keyboard shortcuts to application actions with safe defaults. Works with Angular, React, Vue, Vite, or plain JavaScript.
